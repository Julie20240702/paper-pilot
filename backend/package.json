{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "predev": "sh -c 'pids=$(lsof -tiTCP:3001 -sTCP:LISTEN || true); if [ -n \"$pids\" ]; then kill -9 $pids; fi'",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.78.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "multer": "^2.1.0",
    "pdf-parse": "^2.4.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
